# Dotfiles

### Requirements
* pip
* git

### Initialization
Execute
```
$ $HOME/bin/env_install
```
Everything should go fine. I'm not going to make it easy-installed, so if it's not fine, you just could open `$HOME/bin/env_install` file and watch, what do you need to do.
